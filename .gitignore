# Secrets — never commit these
.env
# spotipy's OAuth token cache (created at runtime when authenticating)
.cache

# Python
__pycache__/
*.py[cod]
.pytest_cache/

# Virtual environment
venv/
.venv/

# Local database and downloaded audio
*.db
downloads/
.DS_Store
