"""
The `library` package is the entire application's core logic.

The TUI (app.py) is the only caller; everything else inside this package is
purposely ignorant of textual so it can be tested offline and reused later.
"""
